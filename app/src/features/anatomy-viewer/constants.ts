/**
 * @fileoverview AI-Primer: Contains shared constants for the anatomy viewer feature.
 * This file centralizes configuration like animation timings to avoid magic numbers
 * in the application logic.
 */

/**
 * The duration in milliseconds for the temporary visual selection effect on a muscle.
 */
export const MUSCLE_SELECTION_FADE_OUT_MS = 750;